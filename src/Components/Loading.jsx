import React from 'react';

const Loading = () => {
    return (
        <div className='flex justify-center item-center'>
            <h1>Loading...</h1>
        </div>
    );
};

export default Loading;